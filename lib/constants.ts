/**
 * Navigation order for page transitions and keyboard/swipe navigation.
 * Order determines transition direction (left/right).
 */
export const NAV_ORDER: string[] = ["/", "/projects", "/about", "/contact"]

export type NavPath = "/" | "/projects" | "/about" | "/contact"
